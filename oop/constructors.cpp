#include <iostream>
#include <string>
using namespace std;

class SmartPhone {
public:
    string brand;
    int batteryLevel;

    // Aufgabe 1: Erstelle einen Standard-Konstruktor (Default)
    // Aufgabe 2: Erstelle einen Konstruktor mit Parametern
    // Aufgabe 3: Erstelle einen Destruktor, der eine Nachricht ausgibt
};

int main() {
    // Aufgabe 4: Erstelle ein Objekt auf dem Stack
    // Aufgabe 5: Erstelle ein Objekt auf dem Heap (new) und lösche es wieder
    return 0;
}
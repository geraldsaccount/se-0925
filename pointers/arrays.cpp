#include <iostream>
using namespace std;

int main()
{
    int arr[] = {10, 20, 30, 40};
    // Aufgabe 1: Erstelle ein Pointer auf das Array

    // Aufgabe 2: Nutze eine while-Schleife und den Pointer p, um alle Elemente des Arrays auszugeben, ohne [] zu benutzen.

    return 0;
}
#include <iostream>
using namespace std;

int main()
{
    int number = 42;
    // Aufgabe 1: Lege eine Variable für den Pointer auf number an

    // Aufgabe 2: Gib den Wert und die Speicheraddresse von number
    // Aufgabe 3: Gib den Inhalt vom Pointer auf number und den Wert von number aus (nutze bei beiden nicht number)

    // Aufgabe 4: Ändere 'number' über den Pointer 'ptr'
    return 0;
}